name: Dependency Review Pipeline

on:
  pull_request:

jobs:
  dependency-review:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull_requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/dependency-review-action@v4